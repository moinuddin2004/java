spring:
  application:
    name: jpa
  datasource:
    url: jdbc:postgresql://localhost:5433/datajpa
    username: chaiaurcode
    password: chaiaurcode
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
