spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  application:
    name: dream-shop
  datasource:
    url: jdbc:postgresql://localhost:5433/dreamshop
    username: chaiaurcode
    password: chaiaurcode
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
api:
  prefix: /api/v1